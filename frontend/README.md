
## Barangay Complaint Management System (Vue)

This frontend is built with Vue 3, Vue Router, Vite, and Tailwind CSS.

## Run locally

- `pnpm install`
- Copy `.env.example` to `.env` if needed and set `VITE_API_BASE_URL`
- `pnpm dev`

## Build

- `pnpm build`
  